<clickhouse>
    <!-- Legacy jwt_validators removed: the OAuth test suite uses token_processors
         configured in config.xml. The previous jwt_validators referenced a
         jwks_server service that does not exist in the Keycloak environment. -->
</clickhouse>