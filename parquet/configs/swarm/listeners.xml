<clickhouse>
    <!--
        Some images do not configure network to allow connections from everywhere - so we add it.
        This configuration is done in most images by default.
    //-->
    <listen_host>::</listen_host>
    <listen_host>0.0.0.0</listen_host>
    <listen_try>1</listen_try>
</clickhouse>
